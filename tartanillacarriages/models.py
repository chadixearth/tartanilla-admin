# This app uses Supabase as the database instead of Django models
# All data operations are handled through the api.tartanilla module

from django.db import models

# No models defined - using Supabase database 